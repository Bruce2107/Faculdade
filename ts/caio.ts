type CT = {
  [key: string]: string;
};
const contentType = {
  '/odonto': 'seg',
  0o1: 'seg',
  a: '32',
};

console.group(contentType['/odonto']);
console.log(Object.keys(contentType));
'c'.toUpperCase();
